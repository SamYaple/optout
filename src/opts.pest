WHITESPACE = _{ " " }

path  = { (!opts ~ ANY)* }

debug = { ("--debug" | "-d") }
help  = { ("--help"  | "-h") }
file  = { ("--file"  | "-f") ~ "="? ~ path }

opts = _{ debug | help | file }

options = _{ SOI ~ opts*? ~ EOI }
