WHITESPACE = _{ " " }

posix_path = @{ "."? ~ ("/" ~ ('a'..'z' | 'A'..'Z' | '0'..'9' | "." | "_" | "-")+ )+ }

debug = { ("--debug" | "-d") }
help  = { ("--help"  | "-h") }
file  = { ("--file"  | "-f") ~ "="? ~ posix_path } 

options = _{ (debug | help | file)+? } 
